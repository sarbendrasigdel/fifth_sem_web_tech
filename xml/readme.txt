// complex type in xml
- any attribute or elements includes

xpath example: userInfo\user\name

XQuery: uses path

File extentions:

 XML -> *.xml
 DTD -> *.dtd  (usecase: #PCDATA, #CDATA )
 XSD -> *.xsd 
 XSL(T) -> *.xsl 
 XQuery -> *.xqy (it uses Xpath to query data from XML structure)